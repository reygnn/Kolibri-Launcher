<?xml version="1.0" encoding="utf-8"?>
<!-- Home page-indicator dot. White to match the launcher-over-wallpaper convention;
     active/inactive is expressed by view alpha + scale in code (not separate drawables). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/white" />
    <size
        android:width="6dp"
        android:height="6dp" />
</shape>
