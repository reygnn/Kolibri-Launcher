<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Theme für alle Activities - Android 16 native -->
    <style name="BaseAppTheme" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="alertDialogTheme">@style/CustomAlertDialog</item>
    </style>

    <style name="AppTheme" parent="BaseAppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="SettingsTheme" parent="BaseAppTheme">
    </style>
    
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?attr/colorSurface</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
    
    <style name="CustomAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="shapeAppearanceOverlay">@style/DialogShapeAppearance</item>
        <item name="buttonBarButtonStyle">@style/DialogButtonStyle</item>
    </style>

    <!-- Wallpaper-following overlays for MainActivity's home-anchored
         dialogs. Picked at show-time based on
         ResolveWallpaperSurfaceUseCase's classification — see
         MainActivity.wallpaperAwareDialogStyle(). The surface / text
         colours mirror the AppDrawer + Customize-dialog plumbing, so
         all three home-anchored surfaces (drawer, layout/colour dialog,
         alert dialogs) stay in sync on the same wallpaper. -->
    <style name="CustomAlertDialog.Light" parent="CustomAlertDialog">
        <item name="colorSurface">@color/app_drawer_surface_light</item>
        <item name="colorSurfaceContainerHigh">@color/app_drawer_surface_light</item>
        <item name="android:colorBackground">@color/app_drawer_surface_light</item>
        <item name="colorOnSurface">@android:color/black</item>
        <item name="colorOnSurfaceVariant">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
    </style>

    <style name="CustomAlertDialog.Dark" parent="CustomAlertDialog">
        <item name="colorSurface">@color/app_drawer_surface_dark</item>
        <item name="colorSurfaceContainerHigh">@color/app_drawer_surface_dark</item>
        <item name="android:colorBackground">@color/app_drawer_surface_dark</item>
        <item name="colorOnSurface">@android:color/white</item>
        <item name="colorOnSurfaceVariant">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>
    
    <style name="DialogShapeAppearance">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">28dp</item>
    </style>
    
    <style name="DialogButtonStyle" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textSize">14sp</item>
        <item name="android:letterSpacing">0.0125</item>
    </style>
    
    <!-- Enter/exit for the swipe-down recent-apps dialog: slides in from the
         top edge so it reads as "pulled down". Applied in
         MainActivity.showRecentAppsDialog via window.setWindowAnimations. -->
    <style name="DialogAnimationFromTop">
        <item name="android:windowEnterAnimation">@anim/recent_apps_enter</item>
        <item name="android:windowExitAnimation">@anim/recent_apps_exit</item>
    </style>

</resources>